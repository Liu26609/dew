import { BaseRequest, BaseResponse, BaseConf } from "./base";

export interface ReqSys extends BaseRequest {
    
}

export interface ResSys extends BaseResponse {
    version:number,
}

export const conf: BaseConf = {
    
}